# For local development (renaming to .env.local)
VITE_API_BASE_URL=http://localhost/Buildyourstudio/backend/api

# For Production (Live Server)
# If the React build is in the root and backend is in /backend, usually leave empty to fallback to default (which is relative path /backend/api)
# Or specify full URL:
# VITE_API_BASE_URL=https://yourdomain.com/backend/api
